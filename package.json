{
  "name": "s-base",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^24.10.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "migrate": "bun run src/database/migrate.ts",
    "newuser": "bun run src/database/create_user.ts"
  }
}
